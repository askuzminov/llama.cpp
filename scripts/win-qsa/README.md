# Проверка префила QSA на Windows

Что нужно сделать на тестовой машине:

1. Скопировать `_local.example.bat` в `_local.bat` и вписать туда путь к модели
   (`MODEL`). `_local.bat` в git не попадает, так что `git pull` его не трогает.
   **Сам `_config.bat` править не надо** - иначе следующий pull упрётся в него.
2. Там же выбрать бэкенд (`CMAKE_BACKEND`) и аргументы запуска (`EXTRA`):
   - Strix Halo, всё на GPU: `-DGGML_VULKAN=ON`, `EXTRA=-ngl 99`
   - 3090 + i9, эксперты на CPU: `-DGGML_CUDA=ON`, `EXTRA=-ngl 99 -ncmoe 30`
3. При необходимости поменять `LOADMODE` - как грузятся веса. По умолчанию
   `-lm dio -lzm dio`: 12 даёт ему 343.3 т/с префила против 259.4 у `-lzm on`
   при той же PPL, и таблица PLE на 26.82 ГиБ остаётся на диске, а не в памяти.
   Проверить варианты можно через `08-model-check.bat`.
   `REPS` - сколько раз llama-bench повторяет каждый замер в 03, 04 и 11.
   По умолчанию 1: llama-bench и так делает прогрев перед каждым замером,
   а на глубине 122880 каждый лишний повтор стоит заметного времени.
4. Переключатели `RUN_*` задают, что гоняет `run-all.bat`. По умолчанию включено
   всё, полный прогон идёт часы; лишнее выключается в `_local.bat`. Значения
   по умолчанию лежат в `_config.bat`.
5. Для шагов 05, 06 и 12 нужен текст wikitext, `run-all.bat` качает его сам.
   Если сеть до huggingface не пускает, положить `wikitext-2-raw-v1.zip`
   рядом со скриптами (распакуется сам) или задать `PPLURL` в `_local.bat`.
   Без текста `run-all.bat` просто пропускает 05, 06 и 12.
6. Запустить `run-all.bat`: он сам собирает проект первым шагом. Отдельно собрать
   можно через `00-build.bat`, тогда в `_local.bat` ставится `RUN_BUILD=0`.

Всё пишется в `logs\`, имена с меткой времени. Логи в git не попадают.
`run-all.bat` кладёт рядом `run-all-<время>-summary.txt` с кодом возврата
каждого шага; ненулевой код значит, что инструмент упал, и надо смотреть его лог.

## Скрипты

| Файл | Нужна модель | Что даёт |
|---|---|---|
| `_local.example.bat` | - | образец локальных настроек; копия под именем `_local.bat` перекрывает `_config.bat` и не отслеживается git |
| `00-build.bat` | нет | сборка с тестами в `BUILD` |
| `run-all.bat` | зависит от `RUN_*` | все шаги с 00 по 15 подряд плюс сводка кодов возврата; сам скачивает текст для 05, 06 и 12. Упавшая сборка останавливает прогон, иначе остальные шаги мерили бы старые бинарники. Не входит только `14-server.bat`: он работает, пока его не остановят |
| `01-fa-correct.bat` | нет | корректность flash attention на разреженных масках формы QSA. Все строки должны быть `OK` |
| `02-fa-perf.bat` | нет | скорость flash attention: плотная маска против разреженной при разных `sparse_blk` и `sparse_grp`, вторым проходом то же на скалярном пути (`FASCALAR=0` его выключает) |
| `03-prefill.bat` | да | префил на глубинах 0 / 32K / 64K / 120K, текущее поведение. Один префил перед развёрткой идёт вхолостую: он платит за чтение модели с диска, если её вытеснил предыдущий шаг |
| `04-fa-sparse.bat` | да | тот же прогон с разреженным путём и без него (`GGML_VK_FA_SPARSE_DISABLE`): выигрывает ли список ячеек у плотного ядра |
| `05-perplexity.bat` | да | perplexity по тексту, отдельно с разреженным путём и без него (`FAVARIANTS`). 06 меряет расстояние между двумя ядрами, а это - расстояние каждого из них до текста |
| `06-kl-divergence.bat` | да | KL-дивергенция разреженного пути относительно плотного ядра. Плечо `0` повторяет базу и даёт уровень шума, плечо `1` - разреженный путь. Требует около 10 ГБ на диске |
| `07-perf-logger.bat` | да | потайминги по операциям Vulkan на глубине `PERFDEPTH` с разреженным путём и без него |
| `08-model-check.bat` | да | почему модель не грузится: `-v`, перебор `-lm` и `-lzm` |
| `09-alloc-timing.bat` | да | из чего складывается время выделения буферов Vulkan и как оно зависит от размера блока |
| `10-load-time.bat` | да | время загрузки модели по парам `-lm`/`-lzm` из `LOADVARIANTS`: полное время прогона, выделение буферов, чтение с диска, и не отображён ли файл (тогда unbuffered чтения того же файла сериализуются) |
| `11-ple-cache.bat` | да | свой кеш строк таблицы PLE (`-lzm dio`) против ленивого mmap (`-lzm on`) на случайных токенах, по одной загрузке модели на вариант из `PLEVARIANTS` |
| `12-ple-real.bat` | да | то же на реальном тексте через `llama-perplexity`: скорость префила и проверка, что PPL у `on` и `dio` совпадает. Прогоняет `dio` по вариантам `PLEREALVARIANTS` (размер блока в байтах, 0 = по умолчанию) |
| `13-disk-iops.bat` | да (как файл для чтения) | свой путь чтения (`LLAMA_ROW_CACHE_BENCH`, развёртка по размеру запроса и глубине очереди за одну загрузку модели) и сырые IOPS диска через diskspd тем же шаблоном доступа. Развёртка идёт `READPATHRUNS` раз подряд (по умолчанию 2). Дальше unbuffered на 32 потока трижды: пока по тому же файлу идёт буферизованное чтение, сразу после его окончания и ещё раз развёртка своего пути. Два необязательных плеча: `RAMMAP` (путь к `RAMMap64.exe`) чистит standby list и даёт по-настоящему холодный страничный кеш, `DEFENDEREXCL=1` исключает файл из антивируса на одну развёртку (нужна консоль с правами администратора). `DISKFILE` в `_local.bat` - если таблица PLE лежит не в первой части сплита (её имя печатает строка `stays on disk in ...`) |
| `14-server.bat` | да | запуск `llama-server` на полном контексте модели (`SRVCTX`, по умолчанию 262144); в `run-all.bat` не входит |
| `15-mmid.bat` | да | настройка `MUL_MAT_ID`: `GGML_VK_MMID_WG256` и `GGML_VK_MMID_SCALE_EPILOGUE` по отдельности и вместе (`MMIDVARIANTS`), плюс `test-backend-ops` на фьюз перед развёрткой |
| `get-wikitext.bat` | нет | текст для 05, 06 и 12 |

## Про разреженный flash attention

Индексер QSA даёт каждому токену свой список блоков кэша, остальное закрыто
маской. Ядро Vulkan умеет этим пользоваться: пре-пасс
`flash_attn_sparse_compact` собирает из строки маски точный список ячеек длиной
`n_kv_max`, и ядро идёт по нему, а не по всему KV. Выбор ячеек ядро не меняет,
поэтому ответ тот же - это чистый вопрос скорости.

Пре-пасс апстримовский (PR #28105). Список строится на строку маски, поэтому
тайл из нескольких строк запроса им пользоваться не может: у каждой строки свой
выбор. На декоде это не мешает - там `gqa_ratio > 1` и тайл это одна строка
запроса на все головы группы. На префиле `gqa_ratio == 1`, и апстрим остаётся
на плотном пути.

Наше дополнение: при `KV >= 8 * n_kv_max` тюнер пересобирает тайл в одну строку
(`block_rows == 1`, это скалярный путь с `block_cols = 64`), и список работает и
на префиле. Тайл в одну строку теряет матричное ядро coopmat, поэтому нужен
порог по глубине: при KV 65536 и бюджете 2048 обход короче в 32 раза, этого
хватает с запасом, на мелких глубинах - нет.
`GGML_VK_FA_SPARSE_DISABLE=1` выключает разреженный путь целиком, а
`GGML_VK_FA_SPARSE_ROW_RATIO` двигает порог (по умолчанию 8).

Второй режим, `GGML_VK_FA_SPARSE_GROUP=1`: пре-пасс объединяет строки маски
всего тайла в один список, тайл остаётся многострочным и матричное ядро
сохраняется. Объединение точное - шейдер по-прежнему применяет маску для каждой
строки за индирекцией списка, поэтому ячейка, которую данная строка не выбрала,
читает `-inf` и в сумму не входит. Ёмкость `min(Br * n_kv_max, KV)` - жёсткая
верхняя оценка, поэтому переполнения и отката на плотный путь нет, а хвост из
`-1` пропускается целым блоком. Памяти столько же:
`CEIL_DIV(nem1, Br) * Br * n_kv_max` против `nem1 * n_kv_max`. Это не тот
прежний гатер, который менял выбор: результат тот же, что у одностроковoго
тайла. `test-backend-ops -o FLASH_ATTN_EXT`: все случаи с `n_kv_max > 0`
проходят в обоих режимах. Скорость меряют 02 (плечо `GGML_VK_FA_SPARSE_GROUP`)
и 04 (плечо `g`), качество - 05 (плечо `g`), и оно обязано совпасть с `1`.

Что было до этого: свой гатер по тайлу. Он сжимал маску в список на группу
строк (объединение их выборов), с ёмкостью с запасом и откатом на плотный путь
при переполнении. На 04, Strix Halo, 14.09, pp2048, `-lzm on` он давал:

| | `-ub 512`, d0 | `-ub 512`, d65536 | `-ub 2048`, d0 | `-ub 2048`, d65536 |
|---|---:|---:|---:|---:|
| гатер по тайлу | 452.9 т/с | 246.4 т/с | 515.1 т/с | 267.6 т/с |
| плотное ядро | 451.8 т/с | 183.2 т/с | 511.9 т/с | 129.5 т/с |

То есть +34% при `-ub 512` и +107% при `-ub 2048` на глубине 65536; по
операциям (07, pp512 на d64000) `FLASH_ATTN_EXT` 695 мс против 1410 мс за 12
вызовов, общее время графа 1.93 с против 2.66 с. Снят он не из-за скорости, а
из-за 06: Mean KLD относительно плотного ядра вышел 0.013247, Same top p 95.2%,
максимальное Δp 56% (PPL при этом не двинулась: 3.8325 против 3.8400). Причину
найти не удалось. Апстримовский список точный - ёмкости и отката у него нет, -
поэтому взят он.

Апстримовский путь, тот же замер (04, 15.09, pp2048, `-lzm dio`):

| | `-ub 512`, d0 | `-ub 512`, d65536 | `-ub 2048`, d0 | `-ub 2048`, d65536 |
|---|---:|---:|---:|---:|
| список на строку маски | 459.87 т/с | 223.88 т/с | 516.34 т/с | 247.40 т/с |
| плотное ядро | 458.58 т/с | 181.82 т/с | 514.29 т/с | 131.31 т/с |

+23% при `-ub 512` и +88% при `-ub 2048`; по операциям (07, pp512 на d64000)
`FLASH_ATTN_EXT` 891 мс против 1426 мс за 12 вызовов, граф 2.12 с против 2.66 с.
Медленнее прежнего гатера, потому что список на строку не даёт пропускать тайл
целиком, зато он точный.

Качество: 06 на новом пути даёт Mean KLD 0.013119, Same top p 95.6% - то же
самое, что у снятого гатера. Это не потеря качества, а расстояние между двумя
ядрами. Прямой замер на `test-backend-ops` (форма модели, hsk=hsv=256, kv=32768,
nb=64, блок 4, скалярный путь, NMSE против CPU):

| бюджет | список | плотное ядро |
|---|---:|---:|
| 2048 | 4.69e-5 | 5.49e-5 |
| 2052 (реальный бюджет модели) | 4.75e-5 | 5.49e-5 |
| 1024 | 2.39e-5 | 2.75e-5 |

Разреженное ядро ближе к точному, чем плотное. Оба лежат в районе 5e-5 NMSE на
узел, то есть около 0.7% относительного RMS, и на 12 слоях QSA расхождение между
ними и даёт 0.013 KLD. По тексту разницы нет: 06 сам печатает
Mean PPL(Q)/PPL(base) 1.000848 +/- 0.001475.

Синтетика 02 на Strix Halo, 15.09, kv=32768, головы 256, бюджет 2048 ячеек.
`grp` - сколько строк маски делят один выбор, `1` это как раз то, что делает
модель, `16` и `64` - потолок, который даёт пропуск тайлов:

| маска | 512 токенов | 2048 токенов |
|---|---:|---:|
| плотная (`n_kv_max=0`) | 51.4 мс | 216.4 мс |
| `grp=1`, блок 1 | 37.4 мс | 160.5 мс |
| `grp=1`, блок 4 | 37.5 мс | 158.1 мс |
| `grp=16`, блок 4 | 21.7 мс | 93.2 мс |
| `grp=64`, блок 4 | 19.1 мс | 83.3 мс |

Список строится на строку, поэтому `grp` на него не влияет, и `grp=16`/`64` тут
показывают не альтернативу, а потолок: пропуск тайла целиком дал бы ещё вдвое.
Второй проход 02 (скалярный путь принудительно) даёт по разреженным случаям те
же числа - разреженный путь и так идёт скалярным ядром, - а плотное ядро на нём
падает с 8.01 до 1.78 TFLOPS.

На CUDA у qwen4exp разреженного пути нет вообще. Он есть
(`ggml_cuda_flash_attn_ext_mma_f16_shall_use_sparse`, гатер индексов вместо
маски), но инстанцируется только при `DKQ/DV` 512/512 или 576/512, то есть на
формах DeepSeek MLA ([fattn-mma-f16.cuh](../../ggml/src/ggml-cuda/fattn-mma-f16.cuh),
`may_use_sparse`); у qwen4exp головы по 256. На 3090 маска QSA ядру не даёт
ничего.

Пары 02 и 04 отвечают на разные вопросы: 02 показывает потолок, который даёт
пропуск тайлов на этом железе, 04 - сколько из него доходит до реальной модели.

## Про `MUL_MAT_ID`

После того как flash attention перестал быть главной статьёй, самая дорогая
операция графа префила - `MUL_MAT_ID`: 507 мс из 2.12 с по логу 07. Два
переключателя, оба портированы из `halo-box/strix-llama.cpp` `f4d6f9e8d`
(там на Qwen3.6-35B-A3B, gfx1151, pp2048 при ub2048 все четыре их флага вместе
дали 1223.75 -> 1733.64 т/с):

- `GGML_VK_MMID_WG256=1` - тюнинг RADV даёт плотному большому тайлу 256 потоков
  на тайл 128x128, а варианты `mul_mat_id` остались на 128. Переключатель
  поднимает до 256 и средний, и большой тайл `mul_mat_id`: те же BM/BN/BK, то
  есть та же разделяемая память, вдвое больше потоков на загрузку тайла A/B,
  вдвое меньше аккумуляторов на поток. У MoE на 256 экспертов при ub=2048 на
  эксперта приходится около 64 строк, поэтому реально работает средний тайл, и
  поднимать надо оба.
- `GGML_VK_MMID_SCALE_EPILOGUE=1` - следующий за `MUL_MAT_ID` узел `MUL`
  (`ffn_moe_weighted`) применяется прямо при записи результата матмула, и запись
  идёт сразу в место назначения `MUL`. Убирает запись и обратное чтение всего
  результата матмула. В апстриме этот фьюз есть, но разрешён только для
  мат-вектора, то есть работает на декоде и не работает на префиле. На coopmat2
  хост отказывает: эпилога в том шейдере нет.

Проверено на Mac (MoltenVK, Intel UHD 630, скалярный путь):
`test-backend-ops test -o MUL_MAT_ID_FUSION` - 13 случаев `OK` с флагом и без,
`GGML_VK_PERF_LOGGER=1` подтверждает, что с флагом фьюз берут и тайловые формы
(`batch=512`), а без него - только мат-векторные. Путь `mul_mmq` (integer dot)
там не проверить, устройство его не поддерживает, - это остаётся на Strix Halo.

## Про hyper-connections

Второй по неэффективности узел графа префила после `MUL_MAT_ID` - проекция
`hc_*_inject`. Она читает тот же поток `[hc * n_embd, n_tokens] = [10240, 512]`,
что и `hc_*_down`, но выдаёт всего `hc = 4` строки, а малый тайл матмула в
Vulkan - 32x32. По логу 07: 95 вызовов по 753 мкс, 71.6 мс из 2.12 с, 55.7
GFLOPS. Соседняя `hc_*_down` на тех же данных с 320 строками - 453 мкс, 7400
GFLOPS. Для сравнения, `RMS_NORM_MUL` на этом же тензоре проходит 21 МиБ за 167
мкс, то есть железо на этой форме даёт полную полосу, а матмул - девятую часть.

Исправление в графе: операнды переставлены, `ggml_mul_mat(xn, w_inject)` даёт
`[n_tokens, hc]`, и бэкенд берёт ядро мат-вектора, потому что столбцов `4 <= 8`.
Обратное транспонирование - несколько КиБ. Числа `test-backend-ops perf` на Mac
(MoltenVK, Intel UHD 630): `m=4 n=512 k=10240` - 8751.68 мкс, переставленная
`m=512 n=4 k=10240` - 984.50 мкс, транспонирование `[512,4] -> [4,512]` - 21.43
мкс. Обе формы и транспонирование добавлены в `test-backend-ops perf -o MUL_MAT`
и `-o CONT`, чтобы их можно было померить на Strix Halo.

В логе 07 после этого строки `MUL_MAT f32 m=4 n=512 k=10240` быть не должно.

## Про `-lzm dio`

Таблица PLE у qwen4exp занимает около 27 ГБ. При `-lzm on` она не грузится в
память, но лежит в отображённом файле: строки достаёт `ggml_get_rows` на CPU,
каждый промах - хардфолт на 4 КиБ, и посреди графа появляется CPU-сплит.

`-lzm dio` вместо этого не создаёт тензор вообще. Строки собирает свой читатель
(`src/llama-row-cache.cpp`): блок равен строке таблицы, промахи читаются
несколькими потоками через `llama_file` буферизованно. Своего кеша блоков нет -
он ничего не давал, числа ниже, - поэтому перед файлом остаётся один страничный
кеш системы: он отдаёт таблице всю свободную память и переживает выход процесса.
В граф уходит уже готовый F32-вход, так что CPU-операции и сплита в середине
графа не остаётся.

Промахи одного гатера сортируются по смещению в файле, и соседние блоки читаются
одним запросом - на реальном тексте соседние строки часто попадают в один блок
или в соседние. Случайное чтение блоками упирается в задержку, а не в полосу, так
что число читателей это и есть глубина очереди к диску; по умолчанию оно берётся
от числа ядер и ограничено диапазоном 8..32.

Замер 15.09 (12 на реальном тексте, 16 чанков по 32768, страничный кеш тёплый):

| режим | префил | PPL | попаданий | прочитано | ожидание чтений |
|---|---:|---:|---:|---:|---:|
| `-lzm on` | 216.18 т/с | 3.9400 | - | - | - |
| `-lzm dio`, пул выключен | 342.15 т/с | 3.9400 | 0.3% | 0.35 ГиБ | 6.56 с |
| `-lzm dio`, пул 8 МиБ | 342.75 т/с | 3.9400 | 34.2% | 0.23 ГиБ | 3.96 с |
| `-lzm dio`, пул 1024 МиБ | 342.60 т/с | 3.9400 | 43.1% | 0.20 ГиБ | 3.51 с |

+58% к префилу при той же PPL. Бюджет пула не значил ничего: между полностью
выключенным пулом и пулом, который держит всё рабочее множество, разница 0.6 т/с
из 342, потому что перед пулом стоит файловый кеш системы и те же страницы
держит он. Ожидание чтений при этом 3.5-6.6 с из 382 с прохода. Рабочее
множество прохода - 2385930 блоков по 90 Б, то есть 0.20 ГиБ. Поэтому пул,
синтаксис `-lzm dio:МиБ`, поле `lazy_cache_mib` и `LLAMA_ROW_CACHE_MIB` удалены.

Блок влияет на объём чтений: из блока в 4 КиБ используется 90 байт. На холодном
страничном кеше числа другие, так что сравнивать надо в одном состоянии кеша.

Небуферизованный вариант, блок 4 КиБ и кеш блоков убраны, числа по ним ниже, в
разделе про отброшенное.

Ручки, все переменные окружения:

| Переменная | По умолчанию | Что делает |
|---|---|---|
| `LLAMA_ROW_CACHE_BLOCK` | размер строки | размер блока в байтах |
| `LLAMA_ROW_CACHE_THREADS` | 8..32 по ядрам | сколько чтений одновременно в полёте |
| `LLAMA_ROW_CACHE_RUN` | 64 | сколько подряд идущих блоков сливать в один запрос |
| `LLAMA_ROW_CACHE_STATS` | выкл | считать рабочее множество (около 48 байт на блок) |

## Что проверено и отброшено

Варианты, которые померены и оказались хуже, вместе с числами. Записаны, чтобы
не пробовать их второй раз. Замеры на Strix Halo, если не сказано иное.

| Вариант | Чем кончилось |
|---|---|
| небуферизованное чтение (direct I/O) в кеше строк | 38.57 с на проход против 25.98 у буферизованного. Глубины очереди не набирает совсем: 6997 запросов/с и на одном читателе, и на 32, при том что diskspd по тому же файлу тем же шаблоном (`-b4K -o1 -t32 -Suw`) даёт 125408 IOPS. Одиночное чтение при этом с diskspd совпадает (6060 против 7066), а после нескольких ГБ чтений по файлу тот же путь выдаёт 71734 запроса/с. Причина не найдена, на машине включён Defender. Код и ручка `LLAMA_ROW_CACHE_BUFFERED` удалены 14.09 |
| блок кеша строк 4 КиБ | скорость та же (25.98 против 26.00 с на проход), но прочитано 4.47 ГиБ против 0.25 ГиБ: из блока используются 90 байт. Умолчание - размер строки |
| свой кеш блоков в кеше строк (любой бюджет) | перед пулом стоит файловый кеш системы, поэтому бюджет почти ни на что не влияет. 12 от 15.09: выключенный пул даёт 342.15 т/с при 0.3% попаданий, 8 МиБ - 342.75 при 34.2%, 1024 МиБ - 342.60 при 43.1%. Разница 0.6 т/с из 342, поэтому пул удалён целиком вместе с `-lzm dio:МиБ` и `LLAMA_ROW_CACHE_MIB` |
| `-lzm off` | таблица PLE целиком в памяти, около 27 ГБ занято без выигрыша по скорости |
| группировка QSA: `g` токенов делят один выбор блоков (`LLAMA_QSA_QGROUP`, `LLAMA_QSA_QWIDEN`) | быстро, но за счёт качества, поэтому удалено 14.09. 07 на глубине 64000, PPL - 05, wikitext, контекст 32768: `1 1` 149.3 т/с при PPL 4.2466; `32 1` 319.8 т/с при +1.04%; `32 2` 283.0 т/с при +0.53%; `32 4` 236.9 т/с при +0.26%. Префил (04, pp2048, `-ub 2048`) на `d65536`: 130.2 против 375.1 / 325.3 / 232.5. Сделать это без потери качества нельзя: общий выбор - это другой выбор |
| группировка QSA "все токены берут выбор последнего токена группы" | PPL 3.94 -> 5.77, Mean KLD 0.595. Была заменена суммой скоров индексера, и вместе с ней удалена |
| скалярный путь flash attention как способ понизить тайл с 16 строк до 4-8 | тайл ниже, но ядро медленнее в разы, и это съедает всё. 02, kv=32768, 512 токенов: плотное 235.4 мс против 52.5 мс на coopmat, `grp=1` 544.0 мс против 91.9. На 2048 токенов 937.9 против 214.6 и 2166.7 против 492.7. Высота тайла - не причина проигрыша `grp=1` |
| свой гатер flash attention по тайлу (список ячеек на группу строк, ёмкость с запасом, откат на плотный путь при переполнении) | быстро (+34% и +107% на префиле, 04 от 14.09), но 06 дал Mean KLD 0.013247 против плотного ядра при неизменной PPL, и причину найти не удалось. Снят 15.09 в пользу апстримовского пре-пасса на строку маски: там ёмкости и отката нет, список точный |
| больше одного 16 МиБ запроса в полёте у загрузчика | один запрос 3.27 ГиБ/с, два 2.98, восемь 2.53 |
| синхронный хендл или запросы по 4 МиБ у загрузчика | 2.66 и 2.67 ГиБ/с против 3.27: нужны и overlapped, и 16 МиБ |

Опровергнутые гипотезы:

- "Буферизованное чтение оставляет по файлу data section, из-за которой все
  дальнейшие некешированные чтения по нему сериализуются". Нет: diskspd даёт те
  же ~125400 IOPS и когда буферизованное плечо шло перед ним (лог 13 от 14:46),
  и когда после (лог 13 от 17:32). Сериализация есть только пока буферизованный
  читатель работает - 489.9 МиБ/с падает до 10.7 МиБ/с при нём и возвращается к
  489.7 сразу после.
- "Свой кеш строк быстрее ленивого mmap примерно на четверть". Артефакт
  холодного страничного кеша: холодный `-lzm on` давал 48.64 с на проход, тёплый
  25.57 против 25.98 у `-lzm dio`. Сравнивать плечи можно только в одном
  состоянии кеша.
- "Выделение буферов Vulkan - половина времени загрузки". Нет: оно почти
  целиком перекрыто чтением. 09 от 15.09: `allocate` 9.20 с и `map` 3.93 с на
  77.11 ГиБ, `buffer allocation took 12.67 s`, чтение 76.23 ГиБ за 23.34 с, а
  вся загрузка - 24.95 с. То есть 36.24 с работы уложились в 24.95 с, буферы
  выделяются на фоне чтения (постадийный загрузчик в
  [src/llama-model.cpp](../../src/llama-model.cpp)), и невперекрытого остаётся
  около 1.4 с. Пол здесь - диск: 3.27 ГиБ/с у загрузчика против 2.31 ГиБ/с у
  diskspd на `-b256K -o32 -t4`, то есть читаем уже быстрее, чем diskspd этим
  шаблоном. Само `vkAllocateMemory` при этом линейно до ~50 ГиБ (около 26 ГБ/с),
  дальше падает до ~3.7 ГБ/с - похоже на исчерпание списка обнулённых страниц у
  Windows, но чинить это незачем, пока оно под чтением.

Ловушки, не варианты:

- Первый прогон модели после шага, который занял память, читает её с диска прямо
  во время замера. Память у GPU общая с системой, 02 забирает под 100 ГБ и
  вытесняет файловый кеш. 03 в прогоне 14.09 дал 231.1 / 163.2 / 250.6 / 171.0
  т/с там, где 04 сразу после него на тех же аргументах дал 452.9 / 246.4 /
  515.1 / 267.6. 03 теперь делает один префил вхолостую перед своей развёрткой.
- 08 и 11 падают на загрузке модели с `vk::Queue::submit: ErrorOutOfDeviceMemory`
  или отказом `vkAllocateMemory`, когда прогоны идут подряд: драйвер освобождает
  память предыдущего процесса не сразу. 07, 10 и 11 ждут `SETTLE` секунд между
  загрузками, 08 не ждёт совсем, а 11 с 30 секундами всё равно упало один раз из
  пяти. Это не свойство проверяемого варианта.
- Раньше `-lzm auto` на Vulkan был равен `-lzm off`: если хоть одно устройство
  сообщает `mmap_support = false`, режим сбрасывался в `off`. Теперь он в такой
  ситуации сводится к `dio` ([src/llama-model.cpp](../../src/llama-model.cpp)),
  то есть умолчание на Strix Halo уже правильное и явный `-lzm` нужен только
  чтобы померить другое плечо.

## Что смотреть в результатах

- `01`: любая строка не `OK` - стоп, дальше смысла нет.
- `02`: сравнить `n_kv_max=0` (плотная) с `sparse_grp=1` - это то, что делает
  модель. Если разреженная строка медленнее плотной, список на этой форме не
  окупается. Список строится на строку маски, так что `sparse_grp=16` и `64`
  больше ничего не показывают. Второй проход - те же случаи на скалярном пути.
- `04`: pp с разреженным путём против `GGML_VK_FA_SPARSE_DISABLE=1`, на
  глубинах `-d 65536` и `-d 122880`. На малой глубине разницы быть не должно,
  там индексер вообще не запускается, и порог `KV >= 8 * n_kv_max` не пройден.
- `05`: PPL по тексту для каждого плеча. Это и есть ответ на вопрос о качестве:
  два плеча должны сойтись в пределах своих `+/-`.
- `06`: сравнивать надо два его плеча между собой. Плечо `0` повторяет базу тем
  же кодом, его Mean KLD - это шум модели; плечо `1` - разреженный путь. База у
  06 - это плотное ядро Vulkan, а не CPU и не текст, поэтому ненулевой KLD тут
  означает только, что два ядра считают по-разному, и сам по себе о качестве не
  говорит; за качеством надо идти в `Mean PPL(Q)/PPL(base)` того же лога и в 05.
- `11`: строка `stays on disk` подтверждает, что таблица не загружена. Дальше по
  порядку: `block lookups hit` - доля попаданий и средний размер запроса;
  `in flight` - сколько запросов реально было в полёте (время внутри читателей,
  делённое на время ожидания). Если при 32 читателях там всё равно ~1, запросы
  сериализуются, и поднимать `LLAMA_ROW_CACHE_THREADS` бесполезно, надо искать
  причину. `per request` - задержка и полоса, `distinct blocks` - рабочее
  множество: бюджет больше него ничего не даст. Сравнивать pp с прогоном
  `-lzm on` из того же лога.
- `15`: четыре плеча, `b` база. Смотреть `pp` на `-ub 2048`: обе ручки трогают
  только тайловый путь `mul_mat_id`, на `-ub 512` и на декоде разницы быть не
  должно. Первым в логе идёт `test-backend-ops`, и если там не все `OK`, числа
  ниже смотреть незачем.
- `12`: то же на реальном тексте. `Final estimate` у `on` и `dio` обязан
  совпадать - это проверка, что кеш отдаёт те же строки. Попадания здесь должны
  быть заметно выше, чем в 11: llama-bench подаёт равномерно случайные id
  токенов, и переиспользовать там почти нечего.
